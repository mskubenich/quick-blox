require 'spec_helper'

describe QuickBlox do
  it 'has a version number' do
    expect(QuickBlox::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
